% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_generation.R
\name{.pick_family_hues}
\alias{.pick_family_hues}
\title{Pick maximally-separated base hues for families}
\usage{
.pick_family_hues(
  n,
  seed,
  hue_range = c(0, 360),
  lightness = 60,
  chroma = 55,
  cvd = "none",
  min_dist = 15,
  max_tries = 20
)
}
\arguments{
\item{n}{Number of families.}

\item{seed}{Random seed controlling the starting hue offset.}

\item{hue_range}{Two-element numeric range (degrees) to spread hues over;
defaults to the full wheel.}

\item{lightness, chroma}{L/C used only to render a representative swatch
for the CVD-distance check; does not affect the returned hues.}

\item{cvd}{\code{"none"} or a CVD type understood by \code{\link[=simulate_cvd]{simulate_cvd()}}.}

\item{min_dist}{Minimum acceptable pairwise CIEDE2000 distance between
representative swatches under simulation.}

\item{max_tries}{Number of alternate offsets to try before giving up and
returning the best one found.}
}
\value{
Numeric vector of \code{n} hues in degrees \verb{[0, 360)}.
}
\description{
Evenly spaces hues around the wheel with a seeded random starting offset,
then (when \code{cvd != "none"}) checks the minimum pairwise perceptual
distance \emph{as it would appear under that color-vision deficiency} and
retries a few alternate offsets if hues that look distinct to full-color
vision would collapse together under simulation.
}
\keyword{internal}
