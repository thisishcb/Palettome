% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_palette_static}
\alias{plot_palette_static}
\title{Static plot of an embedding colored by the current palette}
\usage{
plot_palette_static(
  pdata,
  session,
  cvd = "none",
  point_size = 0.6,
  alpha = 0.7
)
}
\arguments{
\item{pdata}{A \code{palettome_data} object from \code{\link[=as_cluster_data]{as_cluster_data()}}.}

\item{session}{A \code{palettome_session} (from \code{\link[=generate_palette]{generate_palette()}}) supplying
the cluster -> color mapping.}

\item{cvd}{\code{"none"} or a CVD type; when set, points are drawn with
\code{\link[=simulate_cvd]{simulate_cvd()}}-adjusted colors as a colorblind-safe preview.}

\item{point_size, alpha}{Passed through to the plotting backend.}
}
\value{
A \code{ggplot} object if ggplot2 is installed, otherwise \code{NULL}
(invisibly) after drawing directly to the current base graphics device.
}
\description{
Works with zero UI packages installed: uses ggplot2 for a nicer plot if
it happens to be available (it is Suggests-only, never required), and
otherwise falls back to base \code{\link[graphics:plot.default]{graphics::plot()}}. Either way, no Shiny/UI
dependency is ever touched.
}
\details{
For large datasets, consider subsampling \code{pdata$cells} (e.g. stratified
per cluster) before calling this for interactive-style previews, and only
plotting the full data for a final export; see \verb{inst/benchmark/} for the
rendering-strategy discussion behind this recommendation.
}
