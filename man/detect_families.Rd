% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_detection.R
\name{detect_families}
\alias{detect_families}
\title{Detect (or pass through) cluster families}
\usage{
detect_families(
  pdata,
  k = NULL,
  h = NULL,
  dist_method = "euclidean",
  hclust_method = "average",
  connectivity = NULL
)
}
\arguments{
\item{pdata}{A \code{palettome_data} object from \code{\link[=as_cluster_data]{as_cluster_data()}}.}

\item{k, h}{See \code{\link[=cut_families]{cut_families()}}. Ignored when families are explicit.}

\item{dist_method}{Distance metric for centroid-based clustering, passed
to \code{\link[stats:dist]{stats::dist()}}. Ignored if \code{connectivity} is supplied.}

\item{hclust_method}{Agglomeration method, passed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{connectivity}{Optional square cluster-by-cluster connectivity or
similarity matrix (e.g. from a shared-nearest-neighbor graph), with row
and column names matching cluster ids. When supplied, it is converted to
a distance matrix (\verb{1 - normalized similarity}) and used instead of
Euclidean centroid distance.}
}
\value{
A \code{palettome_families} object: a list with
\describe{
\item{assignment}{data frame with columns \code{cluster}, \code{family_id}.}
\item{method}{\code{"explicit"}, \code{"hclust"}, or \code{"graph"}.}
\item{dendro}{The \code{compute_family_dendrogram()} result, or \code{NULL} if
\code{method == "explicit"}; kept so a UI can preview/re-cut the
dendrogram without recomputation.}
\item{neighbors}{Neighbor information ready to pass to
\code{\link[=generate_palette]{generate_palette()}} -- the supplied \code{connectivity} matrix if any,
otherwise the cluster centroids -- so neighbor-aware coloring works
without recomputing anything.}
}
}
\description{
If \code{pdata} carries an explicit family column (i.e. \code{pdata$has_family} is
\code{TRUE}), that mapping is used as-is and no clustering is performed.
Otherwise, families are auto-detected by hierarchical clustering of
cluster centroids (or a supplied connectivity graph). Either way, the
result is meant to be a \emph{starting point}: it is a plain
cluster -> family_id data frame that a UI can freely rewrite by hand
(drag-to-regroup) afterward.
}
