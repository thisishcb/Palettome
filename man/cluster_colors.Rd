% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{cluster_colors}
\alias{cluster_colors}
\title{Get cluster colors as a named vector or a plain data frame}
\usage{
cluster_colors(session, format = c("vector", "data.frame"))
}
\arguments{
\item{session}{A \code{palettome_session} (from \code{\link[=generate_palette]{generate_palette()}}).}

\item{format}{\code{"vector"} (default) for a named character vector keyed by
cluster id, or \code{"data.frame"} for a \code{cluster}/\code{color}/\code{family_id}/
\code{family_color} data frame (the family's own representative color
merged in alongside its id, so grouping/faceting/legend code doesn't
need a separate join against \code{\link[=family_colors]{family_colors()}}).}
}
\value{
A named character vector, or a data frame, depending on \code{format}.
}
\description{
The most common thing to actually do with a \code{palettome_session} is hand
its colors to something else's plotting call (e.g. \code{Seurat::DimPlot(cols = cluster_colors(session))}, \code{ggplot2::scale_color_manual(values = cluster_colors(session))}). This pulls just the \code{cluster -> color}
mapping out of the fuller session object in whichever shape is
convenient, without needing to know the session's internal structure.
}
\examples{
assignment <- data.frame(cluster = paste0("c", 1:4), family_id = rep(c("F1", "F2"), each = 2))
session <- generate_palette(assignment, mode = "harmonious", seed = 1)
cluster_colors(session)
cluster_colors(session, format = "data.frame")
}
