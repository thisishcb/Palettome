% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_detection.R
\name{compute_family_dendrogram}
\alias{compute_family_dendrogram}
\title{Build a cluster-distance dendrogram for family detection}
\usage{
compute_family_dendrogram(
  pdata,
  dist_method = "euclidean",
  hclust_method = "average",
  connectivity = NULL
)
}
\arguments{
\item{pdata}{A \code{palettome_data} object from \code{\link[=as_cluster_data]{as_cluster_data()}}.}

\item{dist_method}{Distance metric for centroid-based clustering, passed
to \code{\link[stats:dist]{stats::dist()}}. Ignored if \code{connectivity} is supplied.}

\item{hclust_method}{Agglomeration method, passed to \code{\link[stats:hclust]{stats::hclust()}}.}

\item{connectivity}{Optional square cluster-by-cluster connectivity or
similarity matrix (e.g. from a shared-nearest-neighbor graph), with row
and column names matching cluster ids. When supplied, it is converted to
a distance matrix (\verb{1 - normalized similarity}) and used instead of
Euclidean centroid distance.}
}
\value{
A list with \code{hclust} (an \link[stats:hclust]{stats::hclust} object), \code{centroids}
(from \code{\link[=compute_centroids]{compute_centroids()}}), and \code{clusters} (the cluster id order used).
}
\description{
Computes cluster centroids (or uses a supplied connectivity/distance
matrix) and runs hierarchical clustering on them. Kept separate from
\code{\link[=detect_families]{detect_families()}} so a UI can render a dendrogram preview once and then
re-cut it at different \code{k}/\code{h} cheaply via \code{\link[=cut_families]{cut_families()}}, without
recomputing distances every time the user drags a cut-height slider.
}
