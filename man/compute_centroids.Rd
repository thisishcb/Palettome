% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_adapters.R
\name{compute_centroids}
\alias{compute_centroids}
\title{Compute per-cluster centroids and cell counts}
\usage{
compute_centroids(pdata)
}
\arguments{
\item{pdata}{A \code{palettome_data} object from \code{\link[=as_cluster_data]{as_cluster_data()}}.}
}
\value{
A data frame with one row per cluster: \code{cluster}, one column per
coordinate named \verb{<coord>_centroid}, \code{n_cells}, and \code{family} if
\code{pdata$has_family} is \code{TRUE}.
}
\description{
Precomputes cluster centroids (mean coordinate per cluster), cell counts,
and (if present) the family each cluster belongs to. Every downstream
interactive operation (recoloring, regrouping) should be able to work off
this small summary rather than touching \code{pdata$cells} again.
}
