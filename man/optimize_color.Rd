% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/color_generation.R
\name{optimize_color}
\alias{optimize_color}
\title{Snap a hand-picked color into the palette's aesthetic envelope}
\usage{
optimize_color(color, session = NULL, target = c("auto", "muted", "vivid"))
}
\arguments{
\item{color}{A hex color string.}

\item{session}{Optional \code{palettome_session} whose current colors define
the target L/C register.}

\item{target}{\code{"auto"} (keep near the input chroma), \code{"muted"}, or
\code{"vivid"}.}
}
\value{
A hex color string with the same hue, adjusted L/C.
}
\description{
Feature companion to manual recoloring: keeps the hue you chose but pulls
its lightness and chroma to something in-gamut and coherent -- either
matched to the lightness/chroma distribution of an existing \code{session}
(so a hand-picked color sits with the rest), or, with no session, toward
the natural lightness for that hue and a pleasing (non-neon) chroma.
}
