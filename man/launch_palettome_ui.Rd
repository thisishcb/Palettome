% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui.R
\name{launch_palettome_ui}
\alias{launch_palettome_ui}
\title{Launch the interactive Palettome Shiny UI}
\usage{
launch_palettome_ui(
  pdata,
  families = NULL,
  session = NULL,
  k = NULL,
  h = NULL,
  connectivity = NULL,
  preview_max_n = 50000,
  ...
)
}
\arguments{
\item{pdata}{A \code{palettome_data} object from \code{\link[=as_cluster_data]{as_cluster_data()}}.}

\item{families}{Optional: a \code{palettome_families} object (from
\code{\link[=detect_families]{detect_families()}}) or a plain \code{cluster}/\code{family_id} data frame to
seed the initial grouping. If \code{NULL}, \code{\link[=detect_families]{detect_families()}} is run with
\code{k}/\code{h}.}

\item{session}{Optional: a \code{palettome_session} (e.g. loaded via
\code{\link[=import_palette_json]{import_palette_json()}}) to resume editing, preserving prior manual
overrides. If \code{NULL}, a fresh harmonious palette is generated.}

\item{k, h}{Passed to \code{\link[=detect_families]{detect_families()}} when \code{families} is \code{NULL}.}

\item{connectivity}{Optional cluster-by-cluster connectivity/distance
matrix passed to \code{\link[=detect_families]{detect_families()}} and used for neighbor-aware
coloring; when \code{NULL}, cluster centroids are used instead.}

\item{preview_max_n}{Passed to \code{\link[=downsample_stratified]{downsample_stratified()}} for the live
scatter plot; the full dataset is only used for the final PNG export.}

\item{...}{Passed on to \code{shiny::runApp()} (e.g. \code{launch.browser}, \code{port}).}
}
\value{
Does not return; runs the Shiny app until interrupted.
}
\description{
This is the \emph{only} function in the package that requires UI packages
(shiny, plotly, colourpicker, shinyjs). All of them are Suggests-only, so
installing/loading the palettome package itself never requires them, and
every other function (\code{\link[=detect_families]{detect_families()}}, \code{\link[=generate_palette]{generate_palette()}},
\code{\link[=plot_palette_static]{plot_palette_static()}}, \code{\link[=export_palette_json]{export_palette_json()}}, ...) works without
them. If any are missing, this fails with a friendly message rather than
an obscure error, and points out that the rest of the package still
works.
}
