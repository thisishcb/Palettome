% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performance.R
\name{downsample_stratified}
\alias{downsample_stratified}
\title{Stratified per-cluster subsample for interactive preview}
\usage{
downsample_stratified(pdata, max_n = 50000, min_per_cluster = 20, seed = 1)
}
\arguments{
\item{pdata}{A \code{palettome_data} object.}

\item{max_n}{Target total number of points after subsampling. If
\code{nrow(pdata$cells) <= max_n}, \code{pdata} is returned unchanged.}

\item{min_per_cluster}{Minimum points kept per cluster even if that
over-represents small clusters relative to a uniform per-cluster share,
so rare clusters stay visible.}

\item{seed}{Random seed for reproducible subsampling.}
}
\value{
A \code{palettome_data} object with a (possibly) reduced \code{cells} table.
}
\description{
Precomputing a small, cluster-stratified subsample is the core trick that
keeps hierarchy editing and recoloring responsive on multi-million-cell
datasets: interactive redraws use this subsample, and only a final export
needs to touch every cell. See \verb{inst/benchmark/} for a benchmark
justifying the approach and default threshold.
}
