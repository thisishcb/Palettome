% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_adapters.R
\name{as_cluster_data}
\alias{as_cluster_data}
\alias{as_cluster_data.data.frame}
\alias{as_cluster_data.Seurat}
\title{Build a palettome data object from a data frame or Seurat object}
\usage{
as_cluster_data(x, ...)

\method{as_cluster_data}{data.frame}(
  x,
  coord_cols,
  cluster_col,
  family_col = NULL,
  cell_id_col = NULL,
  ...
)

\method{as_cluster_data}{Seurat}(
  x,
  reduction = "umap",
  dims = 1:2,
  cluster_col = "seurat_clusters",
  family_col = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{data.frame} (or object coercible to one) or a \code{Seurat} object.}

\item{...}{Passed to methods.}

\item{coord_cols}{Character vector of 2 or 3 column names holding
embedding coordinates (e.g. \code{c("UMAP_1", "UMAP_2")}).}

\item{cluster_col}{Name of the column holding cluster/subtype labels.}

\item{family_col}{Optional name of a column holding an explicit
parent/family label. When \code{NULL} (default), families are left
undetected and must be filled in by \code{\link[=detect_families]{detect_families()}}.}

\item{cell_id_col}{Optional name of a column with unique per-row/cell
identifiers. When \code{NULL}, row numbers are used.}

\item{reduction}{Name of the dimensional reduction to pull coordinates
from (passed to \code{Seurat::Embeddings()}), e.g. \code{"umap"}.}

\item{dims}{Integer vector of length 2 or 3 selecting which dimensions of
\code{reduction} to use.}
}
\value{
A \code{palettome_data} object: a list with elements
\describe{
\item{cells}{A data frame with columns \code{cell_id}, the coordinate
columns (as named in \code{coord_cols}), \code{cluster}, and optionally
\code{family}.}
\item{coord_cols}{Character vector of coordinate column names in
\code{cells}, in order (length 2 or 3).}
\item{has_family}{Logical; whether an explicit family column was
supplied.}
}
}
\description{
\code{as_cluster_data()} normalizes either a plain data frame of embedding
coordinates + cluster labels, or a \code{Seurat} object, into the common
\code{palettome_data} structure that every other core function (
\code{\link[=detect_families]{detect_families()}}, \code{\link[=generate_palette]{generate_palette()}}, \code{\link[=plot_palette_static]{plot_palette_static()}}, ...)
consumes. This is the only place that needs to know about Seurat, and
Seurat is never required to be installed to use the rest of the package.
}
